#include<cstdio>
#include<string>

using namespace std;

string str;

int main(void){
    int ans = 0;
    int cnt2 = 0, cnt1 = 0;
    bool c = false;
    ios_base::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
    cin >> str;
    
    while(!c){
        for(string::iterator iter = str.begin(); iter <= m)
    }

    cout << ans;
}
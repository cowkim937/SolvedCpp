#include<bits/stdc++.h>

#define MAXSIZE (3 * 1000000) + 1

using namespace std;

void quick_sort(int* arr, int left, int right){
    int pivot, temp;
    int low, high;

    low = left;
    high = right + 1;
    pivot = arr[left];
    
}

int main(void){
    ios_base::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
    int n, s[MAXSIZE];

    scanf("%d", &n);

    for(int i=0; i<n; i++)
        scanf("%d", &s[i]);
    
    

    return 0;
}